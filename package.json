{
  "name": "pi-smart-edit",
  "version": "0.1.5",
  "description": "Safer, cheaper edits by verifying prior reads in the harness instead of the prompt. Lets the harness verify the model already read the latest text it wants to edit, avoiding old-text replay and hash-decorated reads.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "extension",
    "smart-edit"
  ],
  "exports": "./index.ts",
  "files": [
    "index.ts",
    "src",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sergiobonfiglio/pi-smart-edit.git"
  },
  "homepage": "https://github.com/sergiobonfiglio/pi-smart-edit#readme",
  "bugs": {
    "url": "https://github.com/sergiobonfiglio/pi-smart-edit/issues"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts",
    "pack:dry": "npm pack --dry-run",
    "release:check": "npm run check && npm run test && npm run pack:dry",
    "publish:public": "npm publish --access public"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "dependencies": {
    "diff": "^8.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0"
  }
}
