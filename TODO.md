# TODO

## Features
- [ ] Add a --replace, --additive, much like https://github.com/deevus/pi-wayfinder
- [ ] Edit huge lines should allow inserting \n
- [ ] Investigate how PDFs, images, and other non-text files should be handled. Comparison to built-in `edit` and `read` behavior.
- [x] Observe complete built-in `grep` results and grep-compatible successful `bash` output for snapshot bookkeeping; `find` and `ls` remain non-snapshot-producing.
